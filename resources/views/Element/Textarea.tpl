<textarea
    name="{$name}"
    id="{$id}"
    class="ai-module__textarea ai-module__field {$class}"
    placeholder="{$placeholder}"
    rows="{$rows}"
    cols="{$cols}"
    {$elementSpecificAttributes}

    {$disabled}
    {$required}
    {$readonly}
>{$value}</textarea>
