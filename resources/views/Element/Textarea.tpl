<textarea
    name="{$name}"
    id="{$id}"
    class="pb-module__textarea pb-module__field {$class}"
    placeholder="{$placeholder}"
    rows="{$rows}"
    cols="{$cols}"
    {$elementSpecificAttributes}

    {$disabled}
    {$required}
    {$readonly}
>{$value}</textarea>
