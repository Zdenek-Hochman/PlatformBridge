<input
    type="{$type}"
    name="{$name}"
    id="{$id}"
    class="ai-form__input {$class}"
    value="{$value}"

    {$fieldSpecificAttributes}

	{$placeholder}

    {$disabled}
    {$required}
    {$readonly}
    {$checked}
    {$autocomplete}
>