<textarea
    name="{$name}"
    id="{$id}"
    class="ai-form__textarea {$class}"
    placeholder="{$placeholder}"
    rows="{$rows}"
    cols="{$cols}"
    {$fieldSpecificAttributes}

    {$disabled}
    {$required}
    {$readonly}
>{$value}</textarea>
